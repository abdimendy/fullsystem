{
  "name": "yellowbook",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  },
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "vercel-build": "npm install --prefix frontend && npm run build --prefix frontend",
    "netlify-build": "node scripts/export-neon-static.mjs && npm install --prefix frontend && npm run build --prefix frontend",
    "sync-netlify-neon": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/sync-netlify-neon.ps1",
    "setup-render": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/setup-render.ps1"
  }
}
