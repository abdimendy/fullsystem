{
  "name": "yellowbook",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "bcryptjs": "^2.4.3",
    "cloudinary": "^2.5.1",
    "jose": "^5.9.6",
    "lambda-multipart-parser": "^1.0.1",
    "pdf-lib": "^1.17.1"
  },
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "vercel-build": "npm install && node scripts/export-neon-static.mjs && npm install --prefix frontend && npm run build --prefix frontend",
    "netlify-build": "npm install && node scripts/export-neon-static.mjs && npm install --prefix frontend && npm run build --prefix frontend",
    "sync-netlify-neon": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/sync-netlify-neon.ps1",
    "setup-render": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/setup-render.ps1"
  }
}
